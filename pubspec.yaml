name: redacted
description: A package that automatically generate a loading widget from your own widget.
version: 1.0.12
homepage: https://github.com/mhdmoh/redacted

environment:
  sdk: '>=3.0.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  measure_size: ^4.0.0
  flutter_svg:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
